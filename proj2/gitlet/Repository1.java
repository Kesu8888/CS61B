package gitlet;
import java.io.File;
public class Repository1 extends Repository{
    /*
    public static void addRemote(String remoteName, String remoteLocation) {
        char[] locationChars= remoteLocation.toCharArray();
        for (int i = 0; i < locationChars.length; i++) {
            if (locationChars[i] == '/') {
                locationChars[i] = File.separatorChar;
            }
        }
        String exactLocation = locationChars.toString() + File.separatorChar + ".gitlet";
        File remoteDir = new File(exactLocation);
        if (!remoteDir.exists()) {
            remoteDir.mkdirs();
        }
        if ()
        {
            remoteDir.mkdirs();
        }
    }
    public static void rmRemote() {

    }
    public static void push() {

    }
    public static void fetch() {

    }
    public static void pull() {

    }

    private static void addRemoteDir(File remoteDir) {
        if (remoteDir.exists()) {
            System.out.println("A remote with that name already exists.");
        } else {
            remoteDir.mkdirs();
        }
    }*/
}
